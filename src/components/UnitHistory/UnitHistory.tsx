/** @jsxImportSource theme-ui */
export function UnitHistory() {

  return (
    <div sx={{display: "flex", justifyContent: "center"}}>
      <section sx={{variant: "containers.singlePageFormCont"}}>
        <p sx={{position: "absolute", top: "100px"}}>
          im some history!
        </p>

      </section>
    </div>

  )
}